# Copia in .env.production (root progetto) — usato solo al deploy, non in dev locale
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require&schema=public"

# Seed (solo se DB vuoto — non crea dati demo, solo cataloghi + purge legacy)
# SEED_ADMIN_EMAIL=admin@coresuite.it
# SEED_ADMIN_PASSWORD=cambia-in-produzione
# SEED_ADMIN_NAME=Amministratore
